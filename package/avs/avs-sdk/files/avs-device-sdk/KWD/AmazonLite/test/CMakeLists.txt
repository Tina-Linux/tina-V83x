set(INCLUDES "${AMAZONLITE_SOURCE_DIR}/include" "${KWD_SOURCE_DIR}/include" "${AMAZONLITE_KEY_WORD_DETECTOR_INCLUDE_DIR}")

set(INPUTFOLDER "${KWD_SOURCE_DIR}/inputs")

discover_unit_tests("${INCLUDES}" AMAZONLITE "${INPUTFOLDER}")