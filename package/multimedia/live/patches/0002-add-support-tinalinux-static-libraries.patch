--- a/config.tinalinux-static-libraries	1970-01-01 08:00:00.000000000 +0800
+++ b/config.tinalinux-static-libraries	2019-03-04 14:31:08.376478321 +0800
@@ -0,0 +1,18 @@
+CROSS_COMPILE?=		$(CROSS)
+COMPILE_OPTS =		$(INCLUDES) -I. -O2 -DSOCKLEN_T=socklen_t -DNO_SSTREAM=1 -D_LARGEFILE_SOURCE=1 -D_FILE_OFFSET_BITS=64
+C =			c
+C_COMPILER =		$(CC)
+C_FLAGS =		$(COMPILE_OPTS) $(CFLAGS)
+CPP =			cpp
+CPLUSPLUS_COMPILER =	$(CXX)
+CPLUSPLUS_FLAGS =	$(COMPILE_OPTS) -Wall -DBSD=1 $(CXXFLAGS)
+OBJ =			o
+LINK =			$(CXX) -o
+LINK_OPTS =
+CONSOLE_LINK_OPTS =	$(LINK_OPTS) $(LDFLAGS)
+LIBRARY_LINK =		$(AR) cr
+LIBRARY_LINK_OPTS =	$(LINK_OPTS)
+LIB_SUFFIX =			a
+LIBS_FOR_CONSOLE_APPLICATION =
+LIBS_FOR_GUI_APPLICATION =
+EXE =
