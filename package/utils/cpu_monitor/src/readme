-o [output dir]
	指定输出文件的路径,注意：如果用到该选项，需要作为第一个参数，例如：cpu_monitor -o output -s
-c [count]
	每隔1s获取CPU的频率、使用率、温度,循环count次
	每个CPU下的三个百分比分别表示：CPU工作时间的占比，CPU空闲时间的占比，CPU等待输入输出完成时间的占比。
-s
	打印CPU的频率、使用率、温度
-l
	列出当前的频率表
